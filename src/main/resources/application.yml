
spring:
  application:
    name: securityPractice

  datasource:
    url: jdbc:mysql://localhost:3306/security?serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update      # ?? ????? update, ?? ??? validate ?? none ??
    show-sql: true           # ???? ?? ?? ??
    properties:
      hibernate:
        format_sql: true     # ?? ?? ?? ???
        dialect: org.hibernate.dialect.MySQL8Dialect

  data:
    redis:
      host: localhost
jwt:
  secret: your-secret-key
  access-token-validity: 3600000  # 1 hour
  refresh-token-validity: 86400000  # 1 day


